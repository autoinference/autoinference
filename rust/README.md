# autoinference (Rust)

Placeholder for the `autoinference` crate. Work in progress.

```bash
cargo add autoinference
```

```rust
println!("{}", autoinference::VERSION);
```

See https://github.com/autoinference/autoinference for source and roadmap.
